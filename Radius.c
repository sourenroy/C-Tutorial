//write a programe to calculate radius of a circel

#include<stdio.h>
int main(){ 
    float radius ;
    printf("enter radius ;");
    scanf("%f", &radius);
    //Value of pi = 3.14 and the formula is 3.14 * radius of circle * radius of circle 
    printf("area is %f", 3.14 * radius * radius);

return 0;
    }
//Write programe using "c" to calculate perimeterof rectangle.
//take side a & b, from user.

#include<stdio.h>
int main(){
    int l , w ;
    printf("enter length :");
    scanf("%d", &l);
    printf("enter width :");
    scanf("%d", &w);
    printf("area is %d" ,(l+w)*2 );
return 0;
}
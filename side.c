// write a programe using "C" to calculate area of a square
#include<stdio.h>
int main() {
    int side;
    printf("enter side :");
    scanf("%d", &side);
    printf("area is ; %d" , side * side);
 return 0;
}

//for floating value use "float side" & "%f" format specifier;